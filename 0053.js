let str = "hello world";
let rem = 'h';

let res = [...str].filter(item => item !== rem);
console.log(res.join(''));

//remove last occurence

//  11. Write a JavaScript program to remove last occurance of a specified character from a given string in JavaScript.